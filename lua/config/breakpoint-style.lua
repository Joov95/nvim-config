-- Define the appearance of breakpoints
vim.fn.sign_define('DapBreakpoint', { text = '🔴', texthl = 'DapBreakpoint', linehl = '', numhl = '' })

-- Set the highlight group for the breakpoint
vim.api.nvim_set_hl(0, 'DapBreakpoint', { fg = '#FF0000', bg = '#31353f' })

-- Set the highlight group for the breakpoint
vim.api.nvim_set_hl(0, 'DapBreakpoint', { fg = '#FF0000', bg = '#31353f' })
